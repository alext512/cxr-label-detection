experiment: {run_name: exp_asl_cosine_ema_fold1_fast, seed: 42, output_root: runs}
data: {root: /path/to/data, csv_name: train1.csv, img_subdir: train1}
model:
  backbone: {name: densenet121, pretrained: true, kwargs: {}}
  head: {name: view_aware_gated_attn, kwargs: {hidden_dim: 512, attn_dim: 256, dropout: 0.30, attn_dropout: 0.10}}
cv: {n_splits: 5, select_fold: 1}
train:
  total_epochs: 10
  frozen_epochs: 1
  head_lr: 0.0003
  backbone_lr: 0.00001
  weight_decay: 0.0001
  accum_steps: 4
  study_batch_size: 48
  max_grad_norm: 1.0
  num_workers: 8
loss: {name: asl, gamma_neg: 4.0, gamma_pos: 1.0, clip: 0.05, eps: 1.0e-8}
scheduler: {name: cosine_warmup, warmup_steps: 80, min_lr_ratio: 0.1}
ema: {enabled: true, decay: 0.999}
augmentations:
  train:
    - {name: resize, size: 512}
    - {name: hflip, p: 0.5}
    - {name: to_tensor}
    - {name: normalize, mean: [0.485, 0.456, 0.406], std: [0.229, 0.224, 0.225]}
  val:
    - {name: resize, size: 512}
    - {name: to_tensor}
    - {name: normalize, mean: [0.485, 0.456, 0.406], std: [0.229, 0.224, 0.225]}
